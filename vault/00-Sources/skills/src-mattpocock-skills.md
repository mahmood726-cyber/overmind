# mattpocock/skills — reusable skill patterns

**URL:** https://github.com/mattpocock/skills
**What:** curated Claude Code skills; starting patterns to ADAPT for our loop templates (not drop-in).

## VERIFIED (fetched repo) — skill → our lane
- **`tdd`** (red-green-refactor loop) → the LFD inner "make tests pass" loop.
- **`diagnosing-bugs`** (reproduce→minimise→hypothesise→instrument→fix→regression-test) → production error
  sweep + T8 failure→regression.
- **`code-review`** (two-axis: Standards + Spec) → cross-vendor / Codex-checker gate.
- **`to-prd`** / **`grill-with-docs`** → journal-upgrade + loss-spec authoring (grilling = rigorous fences).
- **`triage`** (state machine of roles) → clinic monitoring + build-order governance.
- **`improve-codebase-architecture`** (deep scan → HTML report) → RapidMeta QA deep-scan.

## Security caveat
Third-party skills run instructions in the agent — **audit contents before install.** Descriptions
verified; internal implementation NOT audited.

## Ties to
[[src-sanyuan-skills]] (Skill Forge/Review to author+audit) · [[src-loop-library]].
