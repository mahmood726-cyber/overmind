# Evidence subsystem — multi-agent review findings (2026-06-04)

6 review lenses, 40 agents, 34 raw findings -> **31 confirmed / 3 refuted** after adversarial verification.
All work already committed+pushed; these are for a follow-up session. Severity is post-verification (adjusted).

## RESOLVED in fix batch 1 (commit c517d47, overmind master, 2026-06-04)
- [HIGH] eutils live-network path untested -> added urlopen-monkeypatched tests for every fail-closed guard.
- [MEDIUM] live-zero-hits scored below offline floor -> no-hits now triggers offline fallback (stays 2). Regression test added.
- [MEDIUM] `doi:`-prefixed source never resolved -> parsed into typed identifiers. Test added.
- [MEDIUM] NCT can never resolve -> added CorpusRecord.nct + by_nct index + efetch DataBankList extraction (live-verified DAPA-HF->NCT03036124). Tests added.
- [MEDIUM] test_relevance_feedback tautology -> now compares feedback vs no-feedback.
- [LOW] non-numeric year aborted corpus load -> degrades to None. Test added.

## RESOLVED in fix batch 2 (overmind master, 2026-06-04)
Batches A1 + B + D from the next-session worklist (Sentinel batch A2+C tracked separately below).
- [MEDIUM] conftest env-precedence masking -> added `test_env_vars_override_explicit_args` (test_config_paths.py): sets all three OVERMIND_* vars and asserts from_directory honors them over explicit args, independent of the hermetic autouse fixture.
- [LOW] eutils response size cap -> `resp.read(_MAX_BYTES+1)` + oversized-payload raise. Test added.
- [LOW] eutils Retry-After/jitter -> `_parse_retry_after` + `_backoff_delay` (honors bounded Retry-After, adds jitter). Test added.
- [LOW] eutils redirect following -> host-pin: a body served from a host != requested host fails closed. Test added.
- [LOW] eutils PMID validation -> esearch drops non-`^[0-9]+$` ids and caps to limit. Test added.
- [LOW] eutils XML content-type -> expect='xml' now rejects non-`<`-leading bodies; BOM stripped before sniff. Test added.
- [LOW] efetch GET URL length -> POST when id list > 200 (NCBI guidance). Tests added (POST large / GET small).
- [LOW] eutils XXE/expat -> documented expat>=2.4 dependency; size cap bounds worst case.
- [LOW] tiny-corpus asymmetry -> live promotion to 3 now requires size>=5 (same floor as offline). Boundary test added.
- [LOW] screening_extraction content gate -> 2 requires non-empty proposals/validated; 3 requires >=1 CLEAN (non-needs_review) validated record.
- [LOW] outcome_switching.json -> dropped from `_EVIDENCE_FILES` (never produced by exercise_evidence; no longer surfaced as a used artifact).
- [LOW] stale-artifact credit -> docstring states exercise (re)writes every credited artifact each run; exercise=False is the documented "score only what was produced" path.
- [LOW] docstring precision -> empty-success live falls back to offline and never suppresses the floor (documented).
- [LOW] duplicate record_id completeness -> `_dedup_by_record_id` in screen()/ScreeningRun; `duplicates_removed` surfaced. Test added.
- [LOW] prisma `retrieved` default -> documented the optimistic-default asymmetry inline.
- [LOW] extraction estimand-mismatch -> softened wording to "review for inversion ... or legitimate HR-vs-OR divergence" (kept the asserted "contradicts count-derived OR" substring); noted zero-cell skip.
- [LOW] fixture realism -> `test_parse_handles_real_world_variability` (interleaved ArticleId, inline markup, CollectiveName/no-initials authors, missing abstract) without disturbing the shared 2-record fixture.
- [tests] fail-closed floors -> test_fail_closed now also pins screening_extraction/citation_grounding/review_workflow floors.
- [tests] live-failure attempt proof -> test_live_corpus_failure now asserts the live fetch WAS called and the crediting artifact came from provider 'offline-jsonl'.
Suite: 711 passed, 10 skipped, 0 failed.

## STILL OPEN (Sentinel — needs the rule + manifest cycle)
- [MEDIUM] Sentinel claim_grounding: bare `http://` clears the whole doc (false negative).
- [LOW] claim_grounding regex edges: `PMC\d` / bare-DOI over-match, OR/RR/HR + '95% CI' false-positives, EXCLUDE_NAMES static list, BOM/utf-8-sig handling.

---


## HIGH (1)

### [tests] Entire live-network path of eutils.py is untested; the opt-in feature that lifts search_corpus 2->3 has zero coverage
- **file:** `test_eutils.py` — whole file (only parse_efetch_xml is exercised)
- **issue:** test_eutils.py exercises ONLY the pure parse_efetch_xml function. The functions that make this a LIVE feature — _http_get (retry/backoff, HTML-error-page rejection, non-retryable 4xx handling, content-type guarding), esearch (JSON parse, missing esearchresult.idlist fail-closed), efetch, eutils_fetch — have NO tests. These contain the most error-prone lesson-encoded logic: Cloudflare/throttle HTML payload rejection (eutils.py:53-55), the 'expected JSON got X' guard (56-57), the 429-vs-other-4xx branch (62-64), the JSONDecodeError->EutilsError wrap (84-85), the 'missing idlist' raise (87-88). All are claimed by the docstring as fail-closed network discipline yet none are verified. A regression making _http_get accept an HTML throttle page as data, or esearch silently return [] on a malformed response, passes the whole suite green. Since live_corpus=True is the ONLY way search_corpus reaches 3/3, that score's credibility rests on untested code.
- **fix:** Monkeypatch urllib.request.urlopen to return: (a) 200 with <!doctype html> body -> expect EutilsError 'HTML error payload'; (b) 200 Content-Type text/html when expect=json -> EutilsError; (c) 429 then 200 -> succeeds after backoff (assert retry/sleep); (d) 404 -> non-retryable EutilsError, NO retry; (e) esearch JSON missing esearchresult.idlist -> EutilsError; (f) esearch non-JSON bytes -> EutilsError. Stub time.sleep. Most important missing test set in the reviewed work.
- _verifier (high): Verified by direct reading and refuted nothing. test_eutils.py (67 lines) imports ONLY parse_efetch_xml and EutilsError and exercises only the pure, no-network parser. A grep across all of tests/ for _http_get / esearch / efetch / eutils_fetch / urlopen finds no test that patches urllib.request.urlopen against overmind.evidence.eutils — the only urlopen monkeypatches are in test_llm_judge.py again_

## MEDIUM (7)

### [correctness] doi:-prefixed source string is stuffed into record_id and never resolved via the DOI index
- **file:** `grounding.py` — _claim_identifier (lines 113-115) + _CorpusIndex.resolve (lines 74-82)
- **issue:** In _claim_identifier, when source is a bare string the branch `if src.startswith(("pmid:", "doi:")) or src.startswith("NCT"): ident["record_id"] = src.strip()` puts the WHOLE string (including the `doi:` prefix) into ident['record_id']. The resolver _CorpusIndex.resolve then only looks that value up in by_record_id, which is keyed on the record's actual record_id field. Real corpus records use record_id of the form `pmid:<id>` (verified against the bundled seed: ids are `pmid:35228754`, etc.), NOT `doi:<id>`. So a claim whose source is `"doi:10.1038/s41591-021-01659-1"` fails to ground even though a record with exactly that DOI is in the corpus — the `doi:` prefix is never stripped and the value is never matched against by_doi. Confirmed empirically: ground_claims([{source:'doi:'+rec.doi}], corpus) returns grounded_count=0 with reason 'does not resolve', when it should be 1. The `pmid:` case only works by luck because corpus record_ids literally start with `pmid:`; the `NCT` and `doi:` cases under-ground legitimate, resolvable citations. This contradicts the module's correctness goal (a claim cited by a resolvable DOI is reported ungrounded). No test covers the `doi:`-string source path, so it passes CI silently.
- **fix:** In the bare-string branch, only treat a value as record_id if it does NOT start with `doi:` (a DOI is not a record_id key). For `doi:`/`pmid:`/`NCT`-prefixed strings, parse them into the typed identifier dict instead: e.g. strip the `doi:`/`pmid:` prefix and populate ident['doi']/ident['pmid'], or always also run extract_identifiers on the raw string and merge. Then resolve() will hit by_doi/by_pmid. Add a regression test: source='doi:<a doi present in seed>' must ground.
- _verifier (high): Confirmed real and reproduced empirically. In _claim_identifier (grounding.py:113-115), a bare-string source matching `src.startswith(("pmid:", "doi:")) or src.startswith("NCT")` is stuffed verbatim into ident['record_id']. _CorpusIndex.resolve (lines 74-82) then looks record_id up only in by_record_id, which is keyed on rec.record_id. The bundled seed (data/corpus_seed.jsonl, 10 records) confirms_

### [domain_stats] NCT is advertised as a groundable identifier but can NEVER resolve
- **file:** `grounding.py` — _CorpusIndex.build / _CorpusIndex.resolve (lines 56-82); CorpusRecord (corpus.py lines 56-72)
- **issue:** extract_identifiers() (line 43) and _claim_identifier() (line 105) both treat NCT as a first-class citation identifier, and the module docstring lists 'NCT' as a supported binding. But _CorpusIndex only builds by_record_id/by_pmid/by_doi and resolve() only checks record_id, pmid, doi -- there is no by_nct. More fundamentally CorpusRecord (corpus.py) has no nct field at all, so an NCT identifier is structurally unresolvable. A claim citing only an NCT (the most common identifier for a trial-level claim in this RapidMeta/CT.gov domain) is silently reported ungrounded even when the corresponding trial is in the corpus. This is a silent under-claim masquerading as honesty: it isn't 'no record', it's 'this identifier type was never indexed'. The reason string ('does not resolve to any corpus record') is misleading because resolution was never attempted for NCT.
- **fix:** Either (a) add an 'nct' field to CorpusRecord and a by_nct index + resolve() branch, or (b) if NCT grounding is intentionally out of scope, stop emitting nct from extract_identifiers/_claim_identifier and document that only doi/pmid/record_id are resolvable, so the advertised contract matches what can actually resolve. A regression test should assert an NCT-only claim against an NCT-bearing record resolves (or that NCT is explicitly rejected at intake).
- _verifier (high): Verified against source. The finding is accurate on every load-bearing point:

1. NCT is advertised as a groundable identifier: module docstring (grounding.py lines 7, 11) lists "DOI / PMID / NCT / corpus record_id" and "{doi/pmid/nct/record_id}". extract_identifiers (line 52) emits an "nct" key, and _claim_identifier (line 112) includes "nct" in its first-class identifier tuple.

2. NCT can never_

### [honesty_scoring] Live corpus attempt that returns zero hits (no exception) crashes search_corpus to 0, below the offline floor of 2
- **file:** `research_benchmark.py` — exercise_evidence (lines 586-595) + _current_system_scores corpus gate (line 384)
- **issue:** In exercise_evidence, the live path only falls back to offline when CorpusSearch(...).run() RAISES (except Exception -> corpus_ran_live=False). But a valid live query can SUCCEED with zero results: eutils.esearch returns an empty idlist (no error), efetch([]) returns [], so CorpusSearch.run writes corpus_search.json with provider='pubmed-eutils', provider_available=True, corpus_size=0, hit_count=0, and NO exception is raised. Therefore corpus_ran_live=True and the offline fallback (which would yield corpus_size>=5, score 2) NEVER runs. In _current_system_scores the upgrade block is gated on `if corpus and corpus.get('corpus_size', 0) >= 1:` so with corpus_size=0 the whole block is skipped and search_corpus stays at its floor of 0. Net effect: an OPT-IN live attempt that legitimately returns no PubMed hits silently DROPS search_corpus from the honest offline 2 down to 0. This is a fail-closed violation in the opposite direction the comment claims ('the score never rises on a failed live attempt') -- it doesn't rise, but it sinks below the floor the offline seed had earned. The docstring at lines 568-570 over-claims: 'any network/parse error falls back to the offline corpus' -- an empty-but-successful result is neither an error nor a fallback trigger.
- **fix:** Treat a live run with hit_count==0 (or corpus_size==0) as a failed live attempt: set corpus_ran_live=False when the live run returns no hits, so the offline fallback executes and search_corpus keeps its honest floor of 2. Alternatively, in exercise_evidence always run the offline corpus step and only OVERWRITE with the live artifact when the live run produced hits>0.
- _verifier (high): VERIFIED, not refuted. I traced the full control flow and reproduced it end-to-end.\n\nMechanism confirmed link-by-link:\n1. exercise_evidence (research_benchmark.py:587-595): live path sets corpus_ran_live=True whenever CorpusSearch(...).run() does NOT raise. The offline fallback (lines 594-595) only runs when corpus_ran_live is False.\n2. A successful live query with zero results raises nothing:_

### [tests] NCT grounding is claimed but impossible and untested; an NCT-cited claim is always ungrounded
- **file:** `test_grounding.py` — test_extract_identifiers (line 18); no NCT-resolution test exists
- **issue:** extract_identifiers parses NCT IDs and _claim_identifier propagates an nct key, but _CorpusIndex (grounding.py:56-82) builds ONLY by_record_id/by_pmid/by_doi and CorpusRecord (corpus.py:56-72) has NO nct field. Therefore index.resolve() can NEVER resolve an NCT-only claim — it is always reported ungrounded even when the matching trial is in the corpus. No test covers an NCT-only claim against an NCT-bearing record, so this broken identifier path is invisible. The module advertises 'DOI / PMID / NCT / corpus record_id' resolution (docstring line 5, policy string line 160); the NCT half is false and untested.
- **fix:** Add a test: claim source={'nct':'NCT01035255'} against a record carrying that NCT; assert grounded_count==1. It will FAIL today, forcing either (a) add CorpusRecord.nct + _CorpusIndex.by_nct + a resolve() branch, or (b) remove the NCT claim from docstring/policy. The code currently advertises a capability it lacks.
- _verifier (high): VERIFIED, not refuted. Every technical claim holds:\n\n1. CorpusRecord (corpus.py:56-72) has no `nct` field, and from_dict (74-102) does NOT copy raw['nct'] even though it exists — the seed JSONL carries NCT data (grep found 8 records with nct), but it is silently dropped on load.\n2. _CorpusIndex (grounding.py:56-72) builds only by_record_id/by_pmid/by_doi; resolve() (74-82) has only record_id/pm_

### [tests] test_relevance_feedback_changes_ranking is a tautology that never compares feedback vs no-feedback
- **file:** `test_screening.py` — test_relevance_feedback_changes_ranking (lines 57-63)
- **issue:** The test name promises it verifies feedback CHANGES ranking, but line 62 asserts screen(...,seed_includes=['pmid:1']) == screen(...same args...) which is pure determinism (holds even if seed_includes were ignored). Line 63 asserts isinstance(base, list). It NEVER compares `fed` against `base` (the no-feedback ranking). If _relevance_feedback_terms were a no-op or discarded its result, this test would still pass. The actual claimed behavior — folding seed terms into the query reorders/rescore results — is unverified.
- **fix:** Use records where feedback demonstrably matters and assert fed != base, e.g. [p.record_id for p in fed] != [p.record_id for p in base], or that a seed-related record's score/rank improves. Optionally assert _relevance_feedback_terms returns the expected distinctive tokens.
- _verifier (high): Confirmed by reading tests/unit/test_screening.py lines 57-63 and the screening.py/corpus.py implementations.

The test is a tautology exactly as described:
- Line 59 computes `base = screen(recs, query="heart failure")` (no feedback), but `base` is only consumed by the trivial `isinstance(base, list)` check on line 63 (always true — screen() unconditionally returns a list).
- Line 62 asserts `[p._

### [tests] Hermetic conftest fixture globally strips OVERMIND_* env vars, masking regressions in the production wrap precedence path
- **file:** `conftest.py` — _hermetic_overmind_env autouse fixture (lines 33-38)
- **issue:** The fixture unconditionally deletes OVERMIND_CONFIG_DIR/DATA_DIR/DB_PATH before EVERY test. Its own docstring documents that these vars take precedence over explicit from_directory() args — a real production code path used by wrap/activation. By stripping them globally+autouse, the suite can never catch a regression in that precedence except in the single test that re-sets them (test_config_paths). If from_directory's env-precedence handling broke, or a new config field forgot to honor the override, no autouse-covered test would notice. Isolation is fine, but the masking risk is unmitigated.
- **fix:** Keep the stripping, but add an explicit opt-in test that monkeypatch.setenv all three OVERMIND_* vars and asserts from_directory honors them OVER explicit args (the exact precedence the docstring depends on), giving the wrap path a regression guard independent of test_config_paths' scope. Verify test_config_paths covers all three vars, not just one.
- _verifier (high): VERIFIED and, if anything, understated. All three load-bearing claims check out against the code:

1. The autouse fixture (tests/conftest.py:33-37) unconditionally `monkeypatch.delenv`s OVERMIND_CONFIG_DIR/DATA_DIR/DB_PATH before EVERY test.

2. The env vars genuinely take precedence OVER explicit args in the production path: `AppConfig.from_directory` (overmind/config.py:138-146) does `os.environ_

### [sentinel_rule] Bare http:// / https:// in LOCATOR_RE clears the entire doc — silences the rule on any URL
- **file:** `claim_grounding.py` — LOCATOR_RE (lines 77-81), used at line 115
- **issue:** LOCATOR_RE ends in an unanchored alternative `https?://`. Any URL anywhere in the doc — a tracking pixel, a localhost link, a logo image src in an .html capsule, a `[see](https://...)` markdown link to a non-source page — makes `LOCATOR_RE.search(text)` truthy at line 115 and the whole document is skipped before claim scanning. Verified: `loc('tracking at http://localhost/x') -> 'http://'`. A capsule asserting `HR 0.74` with zero real citations but one boilerplate footer URL (very common in HTML capsules / GitHub Pages templates) escapes the rule entirely. This is the dominant false-NEGATIVE: it guts the rule's recall precisely on the shipped-HTML capsules it most wants to catch.
- **fix:** Drop the bare `https?://` catch-all. Keep only source-shaped URLs: `https?://(?:dx\.)?doi\.org/`, `https?://pubmed\.ncbi\.nlm\.nih\.gov/\d`, `https?://clinicaltrials\.gov/.*NCT\d{8}`, `https?://www\.crd\.york\.ac\.uk/prospero/...`. A generic URL is not a resolvable source identifier.
- _verifier (high): VERIFIED, not refuted. LOCATOR_RE (claim_grounding.py lines 77-81) ends with a bare, unanchored `https?://` alternative. At line 115, `if LOCATOR_RE.search(text): continue` skips the ENTIRE document before any claim scanning runs. I confirmed empirically that bare URLs of every shape match this branch: `http://localhost/x` -> 'http://', a CDN image src `https://cdn.site.com/logo.png` -> 'https://'_

## LOW (23)

### [correctness] Duplicate record_ids in the input break the candidate_count==proposal_count completeness invariant
- **file:** `screening.py` — screen() lines 159-174 (zero-overlap completeness loop) + hit_ids set at 140
- **issue:** screen() builds hit_ids = {h.record.record_id for h in hits} then appends every record whose id is NOT in hit_ids. If the caller passes a raw list containing two records with the same record_id where only one copy scores >0 in BM25 (e.g. different abstracts but same id, or one copy with overlapping terms and one without), the zero-score duplicate is suppressed because its record_id is already in hit_ids — so proposal_count < candidate_count and a record silently disappears from the worklist, violating the documented no-silent-truncation guarantee and ScreeningRun's `candidate_count == proposal_count` assertion. The default OfflineCorpusProvider dedups on record_id so the nightly path is safe, but screen() is a public function called directly in tests and by callers with arbitrary lists. Severity low because it requires same-id records with differing searchable text.
- **fix:** Track completeness by record identity/position rather than by record_id set: iterate `for idx, rec in enumerate(records)` and emit a proposal for every index not already represented by a hit at that position; or dedup the input by record_id at the top of screen() with an explicit warning, matching the provider's contract. Add a test with duplicate record_ids asserting proposal_count == len(records).
- _verifier (high): Confirmed real and empirically reproduced. screen() (screening.py:139-174) builds hit_ids as a SET of record_ids from rank()'s hits, then in the completeness loop (163-174) skips any record whose record_id is already in hit_ids. rank() (corpus.py:269-287) scores every record independently but only emits a CorpusHit when score>0. So when two input records share a record_id and exactly one copy scor_

### [correctness] efetch sends all PMIDs as a GET query string; large id lists can exceed URL length limits / NCBI guidance
- **file:** `eutils.py` — efetch() lines 160-170
- **issue:** efetch joins all pmids into the `id=` parameter of a GET URL. esearch can return up to `retmax` PMIDs (esearch passes retmax=max(1,int(limit)), and limit is caller-controlled — corpus._records_for requests max(limit*3,25), benchmark uses 10, but a direct eutils_fetch(query, limit=large) is possible). For large PMID batches this produces an over-long GET URL; NCBI's documented guidance is to POST efetch when the id list is large. With urllib this can surface as an HTTPError (414) or a truncated request rather than a clean EutilsError tied to the real cause. The opt-in/network path makes this lower severity, but it is a latent correctness/robustness gap.
- **fix:** Switch efetch to POST (urllib.request.Request with data=urlencode(params).encode()) when len(pmids) exceeds a threshold (e.g. >200), or always POST efetch per NCBI guidance. Keep the same HTML/parse fail-closed handling on the response.
- _verifier (medium): Mechanism verified: efetch() (eutils.py:163-170) builds a GET URL with all PMIDs in `id=",".join(pmids)` via urlencode; esearch retmax (line 77 `max(1,int(limit))`) and efetch id-count are caller-controlled with NO hard cap, so a direct `eutils_fetch(query, limit=large)` (corpus.py:238 passes limit through) could create an over-long URL. NCBI does document POST for large id lists, so the suggested_

### [correctness] Non-integer year string raises ValueError that aborts the entire corpus load instead of being a soft/skipped field
- **file:** `corpus.py` — CorpusRecord.from_dict line 95 (year parsing)
- **issue:** from_dict does `year=int(year) if year else None`. If a record's `year` is a non-numeric string (e.g. '2021a', 'in press'), int() raises ValueError. In OfflineCorpusProvider.records() this ValueError is caught and re-raised as a hard 'bad corpus record' that aborts loading the WHOLE corpus (one malformed optional field kills every record). year is an optional, non-groundable field (title/id are the minimum unit), so a bad year should arguably degrade to year=None rather than fail-closed the entire index. Also `year=0` is silently dropped to None via the falsy `if year` test (minor). Severity low because the bundled seed is clean and live efetch only emits year when ytext.isdigit().
- **fix:** Wrap the year coercion in a local try/except mapping non-numeric to None (year is optional), so a malformed optional field does not abort the whole corpus load. Use `year is not None` rather than truthiness if year==0 should be preserved (rare for publication years, so optional).
- _verifier (high): Verified against source. corpus.py:95 does `year=int(year) if year else None`; a truthy non-numeric string ("2021a", "in press") makes int() raise ValueError. OfflineCorpusProvider.records() (corpus.py:176-177) catches ValueError and re-raises it as a hard `{path}:{lineno}: bad corpus record` that propagates out of records() with no per-record skip — so one malformed OPTIONAL field aborts the enti_

### [domain_stats] Inverted-effect check compares a published HR against a count-derived OR (different estimands)
- **file:** `extraction.py` — _check_outcome_coherence inverted-HR block (lines 128-140)
- **issue:** The anti-pattern check derives an OR from the 2x2 (a*d)/(b*c) and compares its side-of-null against outcome['effect']/publishedHR via (effect-1)*(or-1)<0. When the published effect is an HR (the dominant case here: the field is literally named publishedHR and estimandType in the tests is 'HR'), an HR and an OR are not the same measure. For typical risk reductions they agree in direction, so the sign-only test is usually safe, BUT in the high-event-rate / time-varying-hazard regime an HR and an OR derived from cumulative counts can legitimately straddle 1.0 without any data-entry error, producing a false 'inverted/mis-labelled effect' flag. Conversely a genuinely inverted HR whose counts also have a small a or b near zero is skipped entirely (the a>0 and b>0 and c>0 and d>0 guard at line 134 silently drops any 2x2 with a zero cell, exactly the zero-cell case where inversion errors are most damaging). The check is a defensible heuristic but the issue text overstates certainty.
- **fix:** Restrict the comparison to when estimandType is 'OR' (or absent) so HR/RR are not compared against an OR; OR widen to allow zero cells with a 0.5 continuity correction before computing the OR (matching the advanced-stats zero-cell rule: add 0.5 only when >=1 cell is zero) so zero-cell inversions are still caught. Soften the issue wording to 'effect and count-derived OR disagree on direction' without asserting the cause is inversion.
- _verifier (high): Verified against extraction.py lines 128-140 and confirmed in test_extraction.py. The mechanism is exactly as described: line 135 computes count_or=(a*d)/(b*c) and line 136 compares its side-of-null against `effect`, which at line 107 falls back to `publishedHR`. The dominant case (test line 75-82 and _good_binary_trial) carries estimandType='HR', so an HR is genuinely being direction-checked agai_

### [domain_stats] 'retrieved' defaults to True for TA-included records, so a missing retrieval flag is counted as a retrieved+assessable report
- **file:** `prisma.py` — prisma_flow retrieved/not_retrieved (lines 51-53)
- **issue:** not_retrieved is computed as retrieved is False (strict identity), and retrieved is retrieved is not False. This means a TA-included record that simply omits the 'retrieved' key is treated as retrieved (the docstring states this is intentional: 'default True if TA-included'). That is a reasonable convention, but it is the optimistic direction: a record genuinely awaiting retrieval but lacking the flag inflates reports_sought-> retrieved-> (awaiting_assessment or assessed). Given the module's stated 'never overstate inclusions' goal, the asymmetry (awaiting full-text is honest, but awaiting retrieval defaults optimistic) is worth documenting or making symmetric. Arithmetic itself reconciles correctly (sought = retrieved + not_retrieved; retrieved = assessed + awaiting), verified against the test fixture.
- **fix:** Either keep the default but add a note that retrieval-stage awaiting is folded into retrieved, or treat missing 'retrieved' as awaiting (None) distinct from True/False so the retrieval stage is as fail-closed as the full-text stage.
- _verifier (high): Verified against prisma.py lines 50-53 and the test fixture. The finding's mechanical claims are all correct: line 52 uses strict `is False` for not_retrieved, line 53 uses `is not False` for retrieved, so a TA-included record omitting the `retrieved` key (get() -> None) satisfies `None is not False` -> True and is counted as retrieved. The test fixture confirms this: records 4/5/6 omit `retrieved_

### [honesty_scoring] Persisted artifacts from a prior (or live, or separate CLI) run are credited with no freshness/provenance check
- **file:** `research_benchmark.py` — read_evidence_artifacts (lines 544-557), run() exercise=False branch (line 638), build_report
- **issue:** artifacts_dir is the persistent config.data_dir/'artifacts' in production (cli.py line 526), not a tmp dir. read_evidence_artifacts reads whatever JSON files sit under <artifacts>/evidence/ with no timestamp, no run-id, no staleness check. Two concrete honesty failures: (1) A single `overmind corpus-search --live` run (cli.py 532-542) writes corpus_search.json with provider='pubmed-eutils' and hits>0. A SUBSEQUENT `research-benchmark` with NO --live-corpus flag, or even exercise=False, calls read_evidence_artifacts, reads that leftover live artifact, and awards search_corpus=3 (line 388) although no live retrieval ran in this benchmark invocation. The opt-in live gate is bypassed by a stale file. (2) After any exercise=True run, exercise=False re-reads the same artifacts and re-credits the full 3s indefinitely, so the benchmark reports first-class capability for runs where nothing was exercised. The score moves without a backing artifact PRODUCED BY THIS RUN -- only one that exists on disk.
- **fix:** Stamp each artifact with a run_id/generated_at and have build_report only credit artifacts whose run_id matches the current invocation (or whose generated_at is newer than the report start). For the live path specifically, require corpus_search.json to carry a 'ran_live_this_invocation' marker before granting 3. At minimum, in exercise=True mode clear/ignore pre-existing evidence/*.json that this run did not (re)write.
- _verifier (high): The finding's FACTS are accurate but its two concrete HIGH-severity honesty failures are refuted on every production-reachable path; only a latent defensive-depth gap remains.\n\nVerified facts: artifacts_dir is the persistent config.data_dir/'artifacts' (cli.py 526, 541), not a tmp dir; read_evidence_artifacts (research_benchmark.py 544-557) reads any JSON under <artifacts>/evidence/ with no time_

### [honesty_scoring] corpus_size 1-4 with hits>0 from the OFFLINE seed scores 1, but the same tiny size from a LIVE provider scores 3
- **file:** `research_benchmark.py` — _current_system_scores, corpus gate lines 384-394
- **issue:** Line 386 sets score=2 only when size>=5 and hits>0, else 1. Line 388 then promotes to 3 whenever provider is non-offline, provider_available, and hits>0 -- with NO size>=5 requirement. So a live provider returning a single hit (corpus_size could be the candidate pool size, but if the live fetch returns just 1-4 records) jumps straight to first-class 3, while the offline seed needs >=5 to even reach 2. 'First-class documented capability' (SCORE_SCALE[3]) should not be earned by a 1-record live result. The live promotion lacks the same size>=5 floor applied to offline.
- **fix:** Require size>=5 (or a minimum hit threshold) on the live promotion too: `if provider_available and provider not in (None,'offline-jsonl') and size>=5 and hits>0: score=3`. A live provider that returns <5 records is not demonstrating large-index retrieval.
- _verifier (high): VERIFIED against research_benchmark.py lines 384-394 and corpus.py run()/_records_for(). The finding is mechanically correct:\n\n- Line 386: `scores[\"search_corpus\"] = 2 if (size >= 5 and hits > 0) else 1` — the OFFLINE path requires corpus_size>=5 just to reach score 2.\n- Line 388: `if provider_available and provider not in (None,'offline-jsonl') and hits>0: score=3` — the LIVE-promotion-to-3 _

### [honesty_scoring] screening_extraction reaches 3 even though extraction.validated_count can be satisfied by REJECTED-only or zero-real-content batches; screening artifact existence alone lifts to 2
- **file:** `research_benchmark.py` — _current_system_scores screening_extraction gate lines 396-401
- **issue:** Line 399 lifts to 2 if EITHER screening OR extraction artifact merely EXISTS, regardless of content (a screening.json with proposal_count=0, or an extraction.json with validated_count=0 and only rejected records, still flips the floor to 2). Line 400 lifts to 3 when screening AND extraction exist AND extraction.validated_count>0. validated_count counts records that passed the contract validator, which the exercise path feeds from a committed demo fixture (extraction_demo.json) -- so 3 is effectively hardcoded-by-fixture: it rises because a bundled demo file validated, not because real extraction ran on real input. This is the same 'self-demo earns the score' pattern that is honest for offline corpus but is weaker for extraction because validated_count includes auto_extracted needs-review records. The comment 'the capability was built AND exercised' is defensible, but 3/3 here is reachable purely from a static demo fixture with no portfolio signal.
- **fix:** Require non-empty content for the 2-floor (proposal_count>0 for screening, validated_count>0 for extraction) and for the 3 require extraction.validated_count>0 AND screening.proposal_count>0 AND that at least one validated record is NOT needs_review (i.e., a clean extraction), so a fixture of only-flagged records cannot reach first-class. Document that the demo fixture is the exercised input.
- _verifier (high): Partially confirmed after reading research_benchmark.py:396-408, extraction.py, screening.py, and the committed fixture.

CONFIRMED parts:
1. Line 399 (`if screening or extraction: score = max(..., 2)`) lifts the floor to 2 on MERE artifact EXISTENCE with no content gate. read_evidence_artifacts() (line 544-557) populates the key purely from a parseable file on disk, so a screening.json with propo_

### [honesty_scoring] outcome_switching.json is read and surfaced in evidence_artifacts_used but never produced by exercise_evidence
- **file:** `research_benchmark.py` — _EVIDENCE_FILES lines 535-542 vs exercise_evidence lines 559-622
- **issue:** _EVIDENCE_FILES maps outcome_switching.json -> 'outcome_switching' so read_evidence_artifacts will load it and run() adds it to report['evidence_artifacts_used'] (line 643). But exercise_evidence never calls outcome_switching(), so this key can only ever come from a stale file written by a separate `overmind` command in a prior run. It is not used in scoring (no score impact), but it lets the benchmark advertise an 'evidence artifact used' that the current run did not generate -- minor honesty/cleanliness drift consistent with the broader stale-artifact issue.
- **fix:** Either produce outcome_switching as part of exercise_evidence (it has a real, deterministic function) or drop it from _EVIDENCE_FILES so a stale file isn't surfaced as a used artifact.
- _verifier (high): Verified against the source. (1) _EVIDENCE_FILES maps "outcome_switching.json" -> "outcome_switching" (research_benchmark.py line 541). (2) exercise_evidence (lines 559-622) calls corpus, screening, extraction, grounding, and prisma_flow only — it never calls outcome_switching(). Grep confirms outcome_switching.json is written ONLY by prisma.py outcome_switching() (line 127), which exercise_eviden_

### [honesty_scoring] Docstrings assert 'never rises on a failed live attempt' and 'Missing artifact => floor' but the corpus floor is 0 and a successful-empty live run produces a present-but-zero artifact that is silently uncredited
- **file:** `research_benchmark.py` — docstring lines 559-570 and inline comment lines 348-352
- **issue:** The block comment (348-352) says missing artifact => floor (fail-closed), which is correct for absent files. But combined with finding #1, a PRESENT live artifact with corpus_size=0 is treated identically to a missing one (skipped by the >=1 gate) while having already suppressed the offline fallback. The reader is led to believe the worst case is 'stays at offline 2', when the actual worst case is 'drops to 0'. The honesty narrative in the comments is stronger than what the code guarantees on the empty-success path.
- **fix:** After fixing #1, update the docstring to state precisely: an empty or failed live attempt falls back to the offline seed and keeps search_corpus at its offline floor; a present-but-empty corpus artifact is never credited AND never suppresses the offline floor.
- _verifier (medium): VERIFIED as a real but minor documentation-vs-behavior gap; severity stays low. Confirmed the underlying mechanism by reading both files:

exercise_evidence (research_benchmark.py:587-595): when live_corpus=True, it calls CorpusSearch(provider=live_pubmed_provider()).run(topic, 10). A live provider returning ZERO records does NOT raise — corpus.py rank() line 249-250 (`if not records: return []`) _

### [network_security] efetch/esearch read the entire HTTP body with no size cap (oversized-payload claim is unmet)
- **file:** `eutils.py` — _http_get, line 51 (body = resp.read())
- **issue:** The module docstring and the operating-rules comment (lines 15-17) explicitly claim oversized payloads are handled and the code fails closed on them, but `body = resp.read()` reads the full response into memory with no upper bound. There is no Content-Length check, no max-bytes read, and no streaming guard anywhere. A large efetch (e.g. a caller passing many PMIDs, or limit not actually bounding efetch size) or a misbehaving/hostile server on the eutils host can stream arbitrary gigabytes into the process and OOM it. efetch in particular concatenates every PMID from esearch into one request, so a single large esearch limit drives an unbounded XML download. This is the only stated robustness guarantee that the code does not actually implement.
- **fix:** Cap the read: honor a sane `Content-Length` ceiling AND defensively bound the read regardless of header, e.g. `body = resp.read(_MAX_BYTES + 1)` then `if len(body) > _MAX_BYTES: raise EutilsError('oversized payload from ...')`. Pick a ceiling per call type (esearch JSON is tiny; efetch XML scales with PMID count, so derive from limit, e.g. ~64KB * limit, hard-capped).
- _verifier (high): The factual core is correct: eutils.py line 51 does `body = resp.read()` with no size cap — no Content-Length check, no max-bytes read argument, no streaming bound. efetch (line 164 `",".join(pmids)`) does concatenate all esearch PMIDs into one request, so XML size scales with the esearch limit. A defensive `resp.read(_MAX_BYTES + 1)` guard is a legitimate, cheap hardening that fits the module's f_

### [network_security] 429/5xx backoff ignores Retry-After header and has no jitter; total wall time only loosely bounded
- **file:** `eutils.py` — _http_get retry loop, lines 59-66
- **issue:** On a 429 (or 503) NCBI returns a `Retry-After` header indicating the required wait. The code ignores it and uses a fixed `_BACKOFF_BASE * 2**attempt` sleep (0.8s, 1.6s). For NCBI's 3-requests/sec throttle without an API key this fixed schedule can both under-wait (re-tripping the throttle, wasting the 3 attempts) and lacks jitter so concurrent callers synchronize their retries. Worst-case wall time is timeout*3 + 2.4s which is bounded, but the retry is not effective against the actual throttle. Combined with the fact that the HTML-error-payload branch (line 54) raises EutilsError which is then caught and retried, a throttle page consumes all 3 attempts on fixed backoff.
- **fix:** Parse `Retry-After` from the HTTPError/response when present and sleep `max(backoff, retry_after)`, and add a small random jitter to the backoff. Optionally cap total elapsed retry time explicitly.
- _verifier (high): Verified against eutils.py. Every factual claim in the finding holds:

1. Retry-After ignored: TRUE. The retry sleep at line 66 is solely `_BACKOFF_BASE * (2 ** attempt)`. A grep across the entire evidence stack confirms no `Retry-After`/`retry_after`/jitter handling exists anywhere. On a real 429/503, urllib raises `urllib.error.HTTPError` which exposes both `.code` and `.headers` (so Retry-After_

### [network_security] Automatic redirect following is enabled; a 3xx from the response is transparently followed
- **file:** `eutils.py` — _http_get, line 47 (urllib.request.urlopen)
- **issue:** `urllib.request.urlopen` installs the default opener which auto-follows HTTP redirects. The request URL host is hardcoded to eutils.ncbi.nlm.nih.gov (good), but if that endpoint (or an on-path proxy) returns a 3xx, urllib will follow it to an arbitrary host before any of the content-type/HTML-payload defenses run. The `if resp.status != 200` check on line 48 never sees a 3xx because the redirect is resolved first. For a tool that is explicitly opt-in network code feeding a scoring pipeline, silently following redirects off the pinned host weakens the 'never treat an error page as data' guarantee.
- **fix:** Build a custom opener with a no-redirect handler (subclass HTTPRedirectHandler to raise) or assert the final `resp.url` host equals the expected eutils host before reading the body; treat an unexpected final host as EutilsError.
- _verifier (high): Technically accurate and not refuted. eutils.py line 47 calls `urllib.request.urlopen(req, ...)` with the default opener, which includes HTTPRedirectHandler and auto-follows 3xx redirects. Confirmed via grep that no custom opener / no-redirect handler is installed anywhere in overmind/. The `if resp.status != 200` check (line 48) and the body-level defenses (HTML-payload rejection line 54, content_

### [network_security] XML safety depends on the runtime expat version, not an explicit hardened parser
- **file:** `eutils.py` — parse_efetch_xml, line 104 (ET.fromstring)
- **issue:** XXE (external entity / file:// / SSRF) is NOT exploitable here: stdlib xml.etree.ElementTree does not resolve external entities and raises ParseError on external entity refs, which is correctly converted to EutilsError. Billion-laughs/quadratic-entity-expansion is mitigated only by CPython's bundled expat >= 2.4 amplification limits — i.e. the protection is implicit and tied to the interpreter/expat build, not asserted by this code. On an older or vendored expat the internal-entity-expansion blowup would consume memory before the oversized-body cap (which also does not yet exist) could help. This is a defense-in-depth gap, not an active vulnerability on a current CPython 3.13.
- **fix:** Either document the expat>=2.4 dependency explicitly, or parse with a configured parser that disables entity expansion (e.g. an XMLParser whose entity handling is constrained), or pull in defusedxml for the efetch path. At minimum, the byte-size cap from the high finding bounds the worst case.
- _verifier (high): Verified against the actual code (eutils.py:104, parse_efetch_xml) and confirmed by running the behaviors on the live interpreter (CPython 3.13.13, expat 2.7.5).

Accurate claims:
- ET.fromstring is used with no hardened parser. XXE is NOT exploitable: stdlib ElementTree does not resolve external entities; tested `<!ENTITY x SYSTEM \"file:///etc/passwd\">` raises ParseError (\"undefined entity\"),_

### [network_security] PMIDs returned by esearch are not validated before being interpolated into the efetch URL
- **file:** `eutils.py` — esearch lines 86-89; efetch line 164
- **issue:** esearch coerces every idlist element via `str(pmid)` with no shape check, then efetch joins them with ',' into the `id` param. urlencode quotes the result so there is no URL/parameter injection, but a malformed/hostile esearch response (idlist containing non-numeric strings, very long values, or thousands of entries) flows straight into the efetch request, contributing to the unbounded-payload issue and producing silent empty results rather than a clear failure. Fails closed in effect (parse yields no records) but not loudly.
- **fix:** Validate each PMID with a `^[0-9]+$` check (PubMed IDs are numeric) and drop/raise on non-conforming entries; cap idlist length to the requested `limit`.
- _verifier (high): All code-level claims verified accurate. eutils.py line 89 `return [str(pmid) for pmid in idlist]` does only str() coercion with no per-element shape check; line 87-88 validates that idlist is a list but not its element contents; efetch line 164 `",".join(pmids)` and line 169 urlencode interpolate the PMIDs into the URL. The finding correctly concedes there is NO injection risk (urlencode neutrali_

### [network_security] For expect='xml' no content-type validation is done; only an HTML doctype/<html prefix is rejected
- **file:** `eutils.py` — _http_get content-type check, line 56 (esearch JSON only)
- **issue:** The JSON path (esearch) validates both content-type and a leading '{'. The XML path (efetch) only rejects bodies whose first 200 bytes start with '<!doctype html' or '<html'. A non-HTML error body (plain-text 'Server busy', a JSON error, or HTML that does not begin with those exact tokens after lstrip — e.g. a leading XML/UTF-8 BOM, an XML declaration wrapping an error, or '<HEAD>' fragments) would pass the gate and reach ET.fromstring. In practice ET.fromstring then raises ParseError -> EutilsError so it still fails closed, but the 'HTML error payload' guard is narrower than the docstring implies and can miss throttle pages that don't lead with the two checked tokens.
- **fix:** Strip a leading BOM before the prefix check, broaden the HTML sniff (case-insensitive search for '<html' / 'throttl' / 'error' in the first N bytes), and for expect='xml' assert the body sniffs as XML ('<?xml' or '<PubmedArticleSet'/'<' root) before parsing.
- _verifier (high): Every factual claim in the finding is confirmed by reading eutils.py lines 50-58 and parse_efetch_xml (lines 98-106):

1. CONFIRMED: The JSON path (line 56) checks both content-type ("json" not in ctype) AND a leading "{". The XML path has NO equivalent: there is no `expect == "xml"` branch and no content-type assertion. The only guard reaching the efetch/XML body is the generic HTML-doctype sniff_

### [tests] test_live_corpus_failure_falls_back_offline cannot distinguish fail-closed from never-attempted
- **file:** `test_research_benchmark.py` — test_live_corpus_failure_falls_back_offline (lines 116-133)
- **issue:** The test claims to prove the live corpus 'fails CLOSED to the offline seed' but cannot distinguish fail-closed-after-attempt from never-attempted. exercise_evidence wraps the live branch in `except Exception: corpus_ran_live=False` then unconditionally runs the offline CorpusSearch; run() adds another bare except. The only assertion is search_corpus==2, exactly the value the offline path yields regardless of whether broken_live() was ever called or raised. A regression that silently skips the live provider entirely still yields 2 and passes. The test does not assert broken_live was invoked nor that the artifact came from the offline fallback.
- **fix:** Have broken_live's fetch record invocation (flag/Mock) and assert it was called, proving the live attempt happened and was caught. Also assert corpus_search.json provider=='offline-jsonl' (not the live name) to confirm the fallback artifact drove the score.
- _verifier (high): VERIFIED, with one severity-reducing caveat the finding omits.

Core claim confirmed empirically. I instrumented `broken_live`'s fetch with a call counter and ran two scenarios:
- (A) live attempted + raises (the path the test docstring claims to prove): search_corpus=2, provider='offline-jsonl', fetch_calls=1.
- (B) live never attempted (live_corpus=False, simulating a regression that skips the l_

### [tests] efetch XML fixture omits common real-PubMed variability (interleaved ArticleIds, title markup, collective/no-initials authors, missing abstract)
- **file:** `eutils_efetch_sample.xml` — both records; ArticleIdList and Author/Abstract shapes
- **issue:** Schema-faithfulness gaps let the parser pass without exercising real variability: (1) no record whose ArticleIdList interleaves a pii/pmc IdType BEFORE the doi (the IdType.lower()=='doi' loop at eutils.py:125-128 is only tested with doi present/absent, never mis-ordered among siblings). (2) No ArticleTitle with embedded markup (<i>/<sup>) — _text uses itertext so it should flatten, but it is untested. (3) No CollectiveName-only author and no author missing Initials (author loop appends LastName+Initials and skips no-LastName authors — untested). (4) No record with the Abstract element entirely missing (abstract=='' path untested). These shapes are common in real NEJM/Lancet efetch payloads.
- **fix:** Add a third fixture record with an interleaved non-doi ArticleId before the doi, an ArticleTitle containing <i>/<sup> (assert markup flattened), a CollectiveName author and an author missing Initials, and no Abstract element (assert abstract==''). Add corresponding assertions.
- _verifier (high): Verified against tests/data/eutils_efetch_sample.xml, eutils.py, and test_eutils.py. This is a test-coverage/fixture gap, not a code defect — the parser is defensively written so all four shapes are handled correctly; they are simply unexercised. Sub-claim accuracy is mixed:

(1) REFUTED in substance. The finding claims the doi loop (eutils.py:125-128) is 'only tested with doi present/absent, neve_

### [tests] Fail-closed and partial-credit boundaries of the evidence gating are under-asserted
- **file:** `test_research_benchmark.py` — test_fail_closed_no_artifacts_keeps_floor (136-146) and gating boundaries
- **issue:** The fail-closed test only checks search_corpus==0 with exercise=False; it does not assert the OTHER gated dimensions (screening_extraction, citation_grounding, review_workflow) stay at their floors when artifacts are absent — yet the core design claim is 'no score moves without a backing artifact'. A regression crediting screening_extraction=3 from a truthy-but-empty artifact dict would not be caught. Also no test exercises the partial-credit boundary in _current_system_scores: corpus_size>=1 but <5, or hit_count==0 should give search_corpus=1 not 2 (gate at research_benchmark.py:386-388), and that boundary is unverified.
- **fix:** In the exercise=False test, also assert screening_extraction/citation_grounding/review_workflow equal their documented floors and evidence_artifacts_used==[]. Add boundary tests feeding a hand-built corpus_search artifact: corpus_size=3,hit_count=0 -> assert search_corpus==1; corpus_size=6,hit_count=0 -> assert NOT credited 2 (hits>0 required).
- _verifier (high): Verified against the actual code and confirmed all three sub-claims hold.

1) Fail-closed test under-asserts (test_research_benchmark.py:136-146): test_fail_closed_no_artifacts_keeps_floor asserts only search_corpus==0 and evidence_artifacts_used==[]. It does NOT pin the heuristic floors of screening_extraction, citation_grounding, or review_workflow. In this test the project has analysis_focus_ar_

### [sentinel_rule] PMC\d and the bare-DOI alternative clear the doc on non-citation substrings
- **file:** `claim_grounding.py` — LOCATOR_RE alternatives `PMC\d` and `10.\d{4,9}/\S+`
- **issue:** Two adversarial clears confirmed: (1) `loc('the PMC1 committee') -> 'PMC1'` — `PMC` followed by any digit anywhere (committee numbers, product SKUs, 'PMC9' Windows-path fragments) counts as a citation. `loc('C:/PMC9/file') -> 'PMC9'`. (2) `loc('upgraded to 10.1234/version2 build') -> '10.1234/version2'` — the bare-DOI alternative `10.\d{4,9}/\S+` matches any dotted-number/slash token, e.g. software versions, ratios written as `10.1234/...`, file paths. Both produce false-negatives: a claim-bearing doc with no real citation but an incidental `PMC7`/`10.xxxx/...` substring is silently cleared.
- **fix:** Tighten PMC to `PMC\d{5,8}\b` (real PMCIDs are 5-8 digits) and require it as a standalone token. For the bare DOI, require the canonical prefix or a citation context (`doi[:=]\s*10\.` rather than a free-floating `10.\d+/\S+`), or at minimum anchor `\S+` to forbid obvious path/version shapes.
- _verifier (high): Both adversarial clears reproduce EXACTLY against the real LOCATOR_RE (lines 77-81):\n- loc('the PMC1 committee') -> 'PMC1'; loc('C:/PMC9/file') -> 'PMC9'\n- loc('upgraded to 10.1234/version2 build') -> '10.1234/version2'\n\nThe defect is genuine. `PMC\\d` matches 'PMC' + ANY single digit, with NO leading word boundary and re.IGNORECASE, so it also clears on embedded tokens like 'APMC1990' -> 'PMC_

### [sentinel_rule] Uppercase OR/RR/HR adjacent to any decimal, and literal '95 % CI', produce claim false-positives
- **file:** `claim_grounding.py` — CLAIM_PATTERNS[0] (line 65) and [3] (line 68)
- **issue:** Two adversarial FPs confirmed against the stated intent (comment line 60-63 claims case-sensitivity + ratio-shape guards prevent prose matches): (1) `claim('There were 5 OR 3.5 ways') -> matches CLAIM_PATTERNS[0]`. The case-sensitive uppercase guard does NOT help when the surrounding prose legitimately uses uppercase 'OR' as the English conjunction followed by a decimal (enumerations, 'option A OR 3.0x cost', tables). (2) `claim('chapter 95 % CI of the room') -> matches [3]` — `95 % CI` is matched case-insensitively with arbitrary internal whitespace and no requirement that 'CI' mean confidence interval; 'CI' as an acronym (continuous integration, command interpreter) after a '95 %' figure trips it. These are exactly the prose-vs-estimate confusions the rule claims to have closed.
- **fix:** For the OR/RR/HR ratio pattern, require a clearer estimate context: a following parenthesis/CI, a comma, or 'ratio'/'='; or require the value be a plausible ratio (e.g. `0?\.\d` or `[0-9]\.[0-9]`) AND not preceded by a digit token. For '95% CI', require it adjacent to a number range or parenthetical (`95\s*%\s*CI[:\s]*[\[(]?\s*\d`) so 'CI of the room' does not match. These are WARN-only, but the stated FP-discipline goal (line 14) is to reach zero FP before BLOCK promotion — these block that path.
- _verifier (high): Both claimed false positives reproduce exactly against the actual patterns (verified by running the regexes):

1. CLAIM_PATTERNS[0] = `\b(?:aHR|aOR|HR|RR|OR)\b\s*(?:[=:]\s*)?\d+\.\d`. Input `'There were 5 OR 3.5 ways'` matches: uppercase English conjunction "OR" + decimal "3.5". The case-sensitivity guard (line 60-63 comment) only stops lowercase "or"; it does NOT stop uppercase "OR" used as a con_

### [sentinel_rule] EXCLUDE_NAMES is a static 2-entry list; other generated artifacts (findings_diff.md, PROGRESS.md, *.jsonl-derived .md) still self-trip
- **file:** `claim_grounding.py` — EXCLUDE_NAMES (lines 54-57), _iter_doc_files line 94
- **issue:** The rule already learned (comment lines 41-44) that `.workflow-state/findings_diff.md` quoted 160 effect 'claims', and excludes that DIR. But EXCLUDE_NAMES only covers `sentinel-findings.md` and `stuck_failures.md`. Other machine-written capsule-adjacent docs that quote prior effect text — e.g. `PROGRESS.md` (per rules.md every repo has one, gitignored but present on disk and scanned by rglob), `aggregate-findings.md`, Overmind nightly `*.md` reports, `code-review` output — are not excluded and will self-trip with quoted claim text. Per lessons.md this is the recurring 'tool output quoting findings re-trips the rule' pattern. Since this is the second time this exact class is being patched by hardcoding a name, it meets the rules.md 'hardcoded batch list edited >once → parametrise' threshold.
- **fix:** Either (a) skip any doc whose path is gitignored (the rule already has repo context), or (b) detect generated-report shape (e.g. presence of the SKIP_MARKER is already honored — encourage emitters to write it), or (c) move EXCLUDE_NAMES to a small glob set and add `progress.md`, `aggregate-findings.md`, `*nightly*.md`. Prefer (a).
- _verifier (medium): CORE MECHANISM CONFIRMED: RepoContext (sentinel/core/context.py) carries only repo_root — no gitignore or changed-files awareness. _iter_doc_files does root.rglob("*") and skips only EXCLUDE_DIRS / EXCLUDE_NAMES / SKIP_MARKER. So gitignored files present on disk ARE scanned. Decisive proof: both EXCLUDE_NAMES entries (sentinel-findings.md, stuck_failures.md) are themselves gitignored in overmind's_

### [sentinel_rule] errors='replace' can fabricate � that shifts _line_of offsets; no BOM strip
- **file:** `claim_grounding.py` — check() lines 105-108, _line_of line 84-85
- **issue:** Files are read with `errors='replace'`, which substitutes U+FFFD for invalid bytes. That is safe for matching but (minor) a UTF-8 BOM at file start is NOT stripped — a leading BOM becomes part of the text and is fine for regex, but if a doc is actually cp1252/UTF-16 the replacement can corrupt token boundaries (e.g. an em-dash mojibake splitting 'HR—0.74'), causing either spurious matches or misses. Offsets from `m.start()` are character offsets into the possibly-mangled text, so the reported line number stays internally consistent (no crash), so this is low. Worth noting because lessons.md flags cp1252 mojibake as a recurring portfolio issue and this rule will scan those very files.
- **fix:** Optionally try utf-8-sig first (strip BOM), fall back to latin-1 for clearly non-UTF-8 files, and keep errors='replace' only as last resort. Low priority — current behavior does not crash or misreport lines.
- _verifier (high): Verified against claim_grounding.py. Both factual claims hold at face value: line 108 reads `read_text(encoding="utf-8", errors="replace")` with no utf-8-sig/BOM handling, so a genuinely cp1252/UTF-16 doc is decoded lossily with U+FFFD substitution. The finding's most important self-acknowledged point is correct and exonerating: there is NO crash and NO line-number misreport. `_line_of` (line 84-8_

## Refuted (checked and dismissed)
- [domain_stats] Ratio CI is checked for ordering but never for null-containment vs SE consistency, and a missing CI on a ratio outcome is silently accepted
- [network_security] Live-corpus fail-closed sets corpus_ran_live=False but still overwrites the corpus_search.json artifact via the offline fallback — verify the score cannot be credited to a partial live run
- [sentinel_rule] WARN-not-BLOCK is sound; ReDoS is not a concern — but document-level grounding is structurally lossy